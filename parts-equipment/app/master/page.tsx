'use client';
import MasterTable from '@/components/MasterTable';

// The control-tower view (WIP) — every job and where it sits across the whole flow.
export default function MasterPage() {
  return <MasterTable />;
}
