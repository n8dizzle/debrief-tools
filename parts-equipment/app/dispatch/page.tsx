'use client';
import DispatcherBoard from '@/components/DispatcherBoard';

// Standalone tab for the Dispatcher lane (WIP) — the human bookend that routes work in
// and schedules it, sitting in front of the Parts and Warehouse boards.
export default function DispatchPage() {
  return <DispatcherBoard />;
}
